import { WizardDirective } from "./WizardDirective.directive";


describe('WizardDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardDirective(null);
    expect(directive).toBeTruthy();
  });
});
